const HEADER_BUTTON = document.getElementById("header-button");
const POPUP = document.getElementById("popup");
const POPUP_WRAP = document.getElementById("popup-wrap");
const POPUP_CONTENT = document.getElementById("popup-content");
const POPUP_CLOSE = document.getElementById("popup-close");
const SELLING_BUTTON = document.getElementById("selling-button");
const ITEMS_BUTTONS = document.getElementsByClassName("what-we-do__button");
const PACKAGE_BUTTON = document.getElementById("package-button");
const HEADER_BURGER = document.getElementById("header-burger");
const MENU = document.getElementById("menu");
const MENU_CLOSE = document.getElementById("menu-close");
const MENU_POPUP = document.getElementById("menu-popup");
export {
    HEADER_BUTTON,
    POPUP,
    POPUP_WRAP,
    POPUP_CONTENT,
    POPUP_CLOSE,
    SELLING_BUTTON,
    ITEMS_BUTTONS,
    PACKAGE_BUTTON,
    HEADER_BURGER,
    MENU,
    MENU_CLOSE,
    MENU_POPUP
};